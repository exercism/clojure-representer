((do
  (do
   (clojure.core/in-ns 'armstrong-numbers)
   (clojure.core/require
    '[clojure.string :as str]
    '[clojure.math.numeric-tower :as math])
   (if
    (clojure.core/= 'armstrong-numbers 'clojure.core)
    PLACEHOLDER-0
    (do
     (clojure.core/-add-loaded-lib 'armstrong-numbers)
     PLACEHOLDER-0)))
  (def
   PLACEHOLDER-5
   (fn*
    ([PLACEHOLDER-1]
     (let*
      [PLACEHOLDER-2
       (String/valueOf PLACEHOLDER-1)
       PLACEHOLDER-3
       (count PLACEHOLDER-2)
       PLACEHOLDER-4
       (map read-string (str/split PLACEHOLDER-2 (re-pattern "")))]
      (=
       (reduce
        +
        (map (fn* [p1#] (math/expt p1# PLACEHOLDER-3)) PLACEHOLDER-4))
       PLACEHOLDER-1)))))))
