((do
  (do
   (clojure.core/in-ns 'armstrong-numbers)
   (if
    (clojure.core/= 'armstrong-numbers 'clojure.core)
    nil
    (do (clojure.core/-add-loaded-lib 'armstrong-numbers) nil)))
  (def
   PLACEHOLDER-3
   (fn*
    ([PLACEHOLDER-2]
     (def
      PLACEHOLDER-4
      (into
       []
       (map read-string (map str (reverse (str PLACEHOLDER-2))))))
     (loop*
      [PLACEHOLDER-0 (dec (count PLACEHOLDER-4)) PLACEHOLDER-1 0]
      (if
       (not (neg? PLACEHOLDER-0))
       (recur
        (dec PLACEHOLDER-0)
        (+
         PLACEHOLDER-1
         (reduce
          *
          (repeat
           (count PLACEHOLDER-4)
           (get PLACEHOLDER-4 PLACEHOLDER-0)))))
       (if (= PLACEHOLDER-1 PLACEHOLDER-2) true false))))))))
