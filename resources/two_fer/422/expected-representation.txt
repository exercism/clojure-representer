((do
  (do
   (clojure.core/in-ns 'PLACEHOLDER-2)
   (clojure.core/require '[clojure.string :as str])
   (if
    (clojure.core/= 'PLACEHOLDER-2 'clojure.core)
    nil
    (do (clojure.core/-add-loaded-lib 'PLACEHOLDER-2) nil)))
  (def
   PLACEHOLDER-1
   (fn*
    ([PLACEHOLDER-0]
     (if (str/blank? PLACEHOLDER-0) "you" PLACEHOLDER-0))))
  (def
   PLACEHOLDER-2
   (fn*
    ([PLACEHOLDER-0] (format "One for %s, one for me." PLACEHOLDER-0))
    ([] (PLACEHOLDER-2 "you"))))))
