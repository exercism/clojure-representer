((do
  (do
   (clojure.core/in-ns 'PLACEHOLDER-1)
   (if
    (clojure.core/= 'PLACEHOLDER-1 'clojure.core)
    nil
    (do (clojure.core/-add-loaded-lib 'PLACEHOLDER-1) nil)))
  (def
   PLACEHOLDER-1
   (fn*
    ([& PLACEHOLDER-0]
     (if
      (let*
       [and__auto__ (not (empty? PLACEHOLDER-0))]
       (if and__auto__ (= (count PLACEHOLDER-0) 1) and__auto__))
      (str "One for " (first PLACEHOLDER-0) ", one for me.")
      (if :else (str "One for you, one for me.") nil)))))))
