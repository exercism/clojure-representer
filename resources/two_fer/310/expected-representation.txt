((do
  (do
   (clojure.core/in-ns 'PLACEHOLDER-1)
   (clojure.core/require '[clojure.string :as str])
   (if
    (clojure.core/= 'PLACEHOLDER-1 'clojure.core)
    nil
    (do (clojure.core/-add-loaded-lib 'PLACEHOLDER-1) nil)))
  (def
   PLACEHOLDER-1
   (fn*
    ([PLACEHOLDER-0]
     (str/join ["One for " PLACEHOLDER-0 ", one for me."]))
    ([] (str "One for you, one for me."))))))
